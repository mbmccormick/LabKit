# Fixtures

- `synthetic/`: committed, generated reports with fake patients and their expected `ParsedReport` JSON (M2/M3).
- `private/`: real reports for local accuracy runs. **Gitignored. Never commit, copy into tests, snapshot, or log their contents.**
